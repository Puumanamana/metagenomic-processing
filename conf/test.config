params {
    max_cpus = 5
    max_memory = 10.GB
    max_time = 1.h
}

process {
    container = 'nakor/metagenome-assembly'
    executor = 'local'
}

singularity {
    enabled = true
    cacheDir = "$HOME/.singularity_images.cache"
}
